import fs from 'node:fs'

const pagePath = 'src/pages/Settlements.tsx'
const componentPath = 'src/features/settlements/SettlementReconciliationWorkspace.tsx'
let source = fs.readFileSync(pagePath, 'utf8')

const componentImport = "import { SettlementReconciliationWorkspace } from '../features/settlements/SettlementReconciliationWorkspace'\n"
const importAnchor = "import { SettlementActivityWorkspace } from '../features/settlements/SettlementActivityWorkspace'\n"
if (!source.includes(componentImport)) {
  if (!source.includes(importAnchor)) throw new Error('Settlement activity import anchor not found')
  source = source.replace(importAnchor, `${importAnchor}${componentImport}`)
}

const start = '        <TabsContent value="reconciliation" className="mt-0 space-y-6">'
const overallTabsClose = '      </Tabs>\n\n      {exportDialogMeta ? ('
const startIndex = source.indexOf(start)
const closeIndex = source.indexOf(overallTabsClose, startIndex)
if (startIndex < 0 || closeIndex < 0 || closeIndex <= startIndex) {
  throw new Error('Reconciliation workspace boundaries not found')
}
const block = source.slice(startIndex, closeIndex)
if (!block.trimEnd().endsWith('</TabsContent>')) {
  throw new Error('Reconciliation workspace did not end at TabsContent boundary')
}

const component = `import { Download } from 'lucide-react'\nimport { Button } from '../../components/ui/button'\nimport { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../../components/ui/card'\nimport { Input } from '../../components/ui/input'\nimport { Label } from '../../components/ui/label'\nimport { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../../components/ui/select'\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from '../../components/ui/tabs'\nimport type { FinanceReconciliationExceptionRow, FinanceReconciliationRow, FinanceReviewState } from '../../lib/financeReconciliation'\nimport { exceptionSeverityTone, n, reviewTone, type FinanceWorkspaceSide } from './settlementModel'\n\ntype Translate = (key: string, fallback: string, vars?: Record<string, string | number>) => string\ntype ReviewDueFilter = 'all' | 'overdue' | 'due_soon' | 'current' | 'resolved' | 'undated'\ntype ReviewStateFilter = 'all' | FinanceReviewState\ntype ReviewTotals = {\n  original: number\n  netAdjustments: number\n  currentLegal: number\n  settled: number\n  outstanding: number\n  overSettled: number\n  exceptionCount: number\n  overdueCount: number\n  reviewCount: number\n}\ntype ReviewStateCounts = {\n  exception: number\n  overdue: number\n  attention: number\n  open: number\n  resolved: number\n}\n\ntype SettlementReconciliationWorkspaceProps = {\n  tt: Translate\n  loading: boolean\n  reconciliationViewsUnavailable: boolean\n  workspaceSide: FinanceWorkspaceSide\n  reviewSide: FinanceReconciliationRow['ledger_side']\n  filteredReviewRows: FinanceReconciliationRow[]\n  filteredReviewExceptions: FinanceReconciliationExceptionRow[]\n  reviewTotals: ReviewTotals\n  reviewStateCounts: ReviewStateCounts\n  reviewSearch: string\n  reviewPartyFilter: string\n  reviewCurrencyFilter: string\n  reviewDueFilter: ReviewDueFilter\n  reviewStateFilter: ReviewStateFilter\n  reviewFromDate: string\n  reviewToDate: string\n  reviewPartyOptions: string[]\n  reviewCurrencyOptions: string[]\n  money: (amount: number) => string\n  duePositionLabel: (position?: FinanceReconciliationRow['due_position'] | null) => string\n  agingBucketLabel: (bucket?: FinanceReconciliationRow['aging_bucket'] | null) => string\n  reviewStateLabel: (state?: FinanceReviewState | null) => string\n  exceptionLabel: (code?: string | null) => string\n  exceptionGroupLabel: (group?: FinanceReconciliationExceptionRow['exception_group'] | null) => string\n  resolutionContextLabel: (row: FinanceReconciliationRow) => string\n  setReviewSearch: (value: string) => void\n  setReviewPartyFilter: (value: string) => void\n  setReviewCurrencyFilter: (value: string) => void\n  setReviewDueFilter: (value: ReviewDueFilter) => void\n  setReviewStateFilter: (value: ReviewStateFilter) => void\n  setReviewFromDate: (value: string) => void\n  setReviewToDate: (value: string) => void\n  onWorkspaceSideChange: (side: FinanceWorkspaceSide) => void\n  onExportReconciliation: () => void\n  onViewAnchor: (anchorKind: FinanceReconciliationRow['anchor_kind'], anchorId: string) => void\n}\n\nexport function SettlementReconciliationWorkspace({\n  tt,\n  loading,\n  reconciliationViewsUnavailable,\n  workspaceSide,\n  reviewSide,\n  filteredReviewRows,\n  filteredReviewExceptions,\n  reviewTotals,\n  reviewStateCounts,\n  reviewSearch,\n  reviewPartyFilter,\n  reviewCurrencyFilter,\n  reviewDueFilter,\n  reviewStateFilter,\n  reviewFromDate,\n  reviewToDate,\n  reviewPartyOptions,\n  reviewCurrencyOptions,\n  money,\n  duePositionLabel,\n  agingBucketLabel,\n  reviewStateLabel,\n  exceptionLabel,\n  exceptionGroupLabel,\n  resolutionContextLabel,\n  setReviewSearch,\n  setReviewPartyFilter,\n  setReviewCurrencyFilter,\n  setReviewDueFilter,\n  setReviewStateFilter,\n  setReviewFromDate,\n  setReviewToDate,\n  onWorkspaceSideChange,\n  onExportReconciliation,\n  onViewAnchor,\n}: SettlementReconciliationWorkspaceProps) {\n  const updateWorkspaceQuery = ({ side }: { side: FinanceWorkspaceSide }) => onWorkspaceSideChange(side)\n  const setExportRequest = (_request: { kind: 'reconciliation' }) => onExportReconciliation()\n  const viewReconciliationAnchor = onViewAnchor\n\n  return (\n${block}\n  )\n}\n`
fs.writeFileSync(componentPath, component)

const replacement = `        <SettlementReconciliationWorkspace\n          tt={tt}\n          loading={loading}\n          reconciliationViewsUnavailable={reconciliationViewsUnavailable}\n          workspaceSide={workspaceSide}\n          reviewSide={reviewSide}\n          filteredReviewRows={filteredReviewRows}\n          filteredReviewExceptions={filteredReviewExceptions}\n          reviewTotals={reviewTotals}\n          reviewStateCounts={reviewStateCounts}\n          reviewSearch={reviewSearch}\n          reviewPartyFilter={reviewPartyFilter}\n          reviewCurrencyFilter={reviewCurrencyFilter}\n          reviewDueFilter={reviewDueFilter}\n          reviewStateFilter={reviewStateFilter}\n          reviewFromDate={reviewFromDate}\n          reviewToDate={reviewToDate}\n          reviewPartyOptions={reviewPartyOptions}\n          reviewCurrencyOptions={reviewCurrencyOptions}\n          money={money}\n          duePositionLabel={duePositionLabel}\n          agingBucketLabel={agingBucketLabel}\n          reviewStateLabel={reviewStateLabel}\n          exceptionLabel={exceptionLabel}\n          exceptionGroupLabel={exceptionGroupLabel}\n          resolutionContextLabel={resolutionContextLabel}\n          setReviewSearch={setReviewSearch}\n          setReviewPartyFilter={setReviewPartyFilter}\n          setReviewCurrencyFilter={setReviewCurrencyFilter}\n          setReviewDueFilter={setReviewDueFilter}\n          setReviewStateFilter={setReviewStateFilter}\n          setReviewFromDate={setReviewFromDate}\n          setReviewToDate={setReviewToDate}\n          onWorkspaceSideChange={(side) => updateWorkspaceQuery({ side })}\n          onExportReconciliation={() => setExportRequest({ kind: 'reconciliation' })}\n          onViewAnchor={viewReconciliationAnchor}\n        />\n\n`
source = `${source.slice(0, startIndex)}${replacement}${source.slice(closeIndex)}`

source = source.replace('  exceptionSeverityTone,\n', '')
source = source.replace('  reviewTone,\n', '')

fs.writeFileSync(pagePath, source)
console.log('Extracted Settlements reconciliation workspace component')
