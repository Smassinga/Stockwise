import fs from 'node:fs'

const pagePath = 'src/pages/Settlements.tsx'
const componentPath = 'src/features/settlements/SettlementExposureWorkspace.tsx'
let source = fs.readFileSync(pagePath, 'utf8')

const componentImport = "import { SettlementExposureWorkspace } from '../features/settlements/SettlementExposureWorkspace'\n"
const importAnchor = "import { SettlementReconciliationWorkspace } from '../features/settlements/SettlementReconciliationWorkspace'\n"
if (!source.includes(componentImport)) {
  if (!source.includes(importAnchor)) throw new Error('Settlement reconciliation import anchor not found')
  source = source.replace(importAnchor, `${importAnchor}${componentImport}`)
}

const start = '        <TabsContent value="exposure" className="mt-0 space-y-6">'
const next = '        <TabsContent value="receipts" className="mt-0 space-y-6">'
const startIndex = source.indexOf(start)
const nextIndex = source.indexOf(next, startIndex)
if (startIndex < 0 || nextIndex < 0 || nextIndex <= startIndex) {
  throw new Error('Exposure workspace boundaries not found')
}
const block = source.slice(startIndex, nextIndex)
if (!block.trimEnd().endsWith('</TabsContent>')) {
  throw new Error('Exposure workspace did not end at TabsContent boundary')
}

const component = `import { Download } from 'lucide-react'\nimport { FinanceSummaryBand } from '../../components/finance/FinanceSummaryBand'\nimport { PremiumStatePanel } from '../../components/premium/PremiumEmptyState'\nimport { Button } from '../../components/ui/button'\nimport { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../../components/ui/card'\nimport { Input } from '../../components/ui/input'\nimport { Label } from '../../components/ui/label'\nimport { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../../components/ui/select'\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from '../../components/ui/tabs'\nimport type { SettlementKind } from '../../lib/orderFinance'\nimport { dueTone, isFinanceDocumentRow, statusTone, type CustomerReceiptCustomer, type CustomerUnappliedCredit, type FinanceWorkspaceSide, type SettlementRow } from './settlementModel'\n\ntype Translate = (key: string, fallback: string, vars?: Record<string, string | number>) => string\ntype DueFilter = 'all' | 'overdue' | 'due_soon' | 'current'\ntype ExposureTab = 'receive' | 'pay'\ntype BridgeTotals = {\n  originalBase: number\n  creditedBase: number\n  debitedBase: number\n  currentLegalBase: number\n  settledBase: number\n  outstandingBase: number\n}\n\ntype SettlementExposureWorkspaceProps = {\n  tt: Translate\n  lang: string\n  loading: boolean\n  stateViewsUnavailable: boolean\n  workspaceSide: FinanceWorkspaceSide\n  rows: { receive: SettlementRow[]; pay: SettlementRow[] }\n  receiveTotal: number\n  payTotal: number\n  overdueCount: number\n  requestedExposureCustomer: CustomerReceiptCustomer | null\n  requestedExposureOutstanding: number\n  requestedExposureInvoiceOutstanding: number\n  requestedExposureUnapplied: CustomerUnappliedCredit | null\n  tab: ExposureTab\n  search: string\n  partyFilter: string\n  statusFilter: string\n  currencyFilter: string\n  dueFilter: DueFilter\n  fromDate: string\n  toDate: string\n  partyOptions: string[]\n  currencyOptions: string[]\n  currentRows: SettlementRow[]\n  filteredRows: SettlementRow[]\n  filteredBridgeTotals: BridgeTotals\n  canManageSettlement: boolean\n  money: (amount: number) => string\n  settlementActionLabel: (kind: SettlementKind) => string\n  viewAnchorLabel: (kind: SettlementKind) => string\n  setSearch: (value: string) => void\n  setPartyFilter: (value: string) => void\n  setStatusFilter: (value: string) => void\n  setCurrencyFilter: (value: string) => void\n  setDueFilter: (value: DueFilter) => void\n  setFromDate: (value: string) => void\n  setToDate: (value: string) => void\n  onWorkspaceSideChange: (side: FinanceWorkspaceSide) => void\n  onExportExposure: () => void\n  onOpenSettlement: (row: SettlementRow, tab: 'settle' | 'history') => void\n  onViewOrder: (row: SettlementRow) => void\n}\n\nexport function SettlementExposureWorkspace({\n  tt,\n  lang,\n  loading,\n  stateViewsUnavailable,\n  workspaceSide,\n  rows,\n  receiveTotal,\n  payTotal,\n  overdueCount,\n  requestedExposureCustomer,\n  requestedExposureOutstanding,\n  requestedExposureInvoiceOutstanding,\n  requestedExposureUnapplied,\n  tab,\n  search,\n  partyFilter,\n  statusFilter,\n  currencyFilter,\n  dueFilter,\n  fromDate,\n  toDate,\n  partyOptions,\n  currencyOptions,\n  currentRows,\n  filteredRows,\n  filteredBridgeTotals,\n  canManageSettlement,\n  money,\n  settlementActionLabel,\n  viewAnchorLabel,\n  setSearch,\n  setPartyFilter,\n  setStatusFilter,\n  setCurrencyFilter,\n  setDueFilter,\n  setFromDate,\n  setToDate,\n  onWorkspaceSideChange,\n  onExportExposure,\n  onOpenSettlement,\n  onViewOrder,\n}: SettlementExposureWorkspaceProps) {\n  const updateWorkspaceQuery = ({ side }: { side: FinanceWorkspaceSide }) => onWorkspaceSideChange(side)\n  const setExportRequest = (_request: { kind: 'exposure' }) => onExportExposure()\n  const openSettlement = onOpenSettlement\n  const viewOrder = onViewOrder\n\n  return (\n${block}\n  )\n}\n`
fs.writeFileSync(componentPath, component)

const replacement = `        <SettlementExposureWorkspace\n          tt={tt}\n          lang={lang}\n          loading={loading}\n          stateViewsUnavailable={stateViewsUnavailable}\n          workspaceSide={workspaceSide}\n          rows={rows}\n          receiveTotal={receiveTotal}\n          payTotal={payTotal}\n          overdueCount={overdueCount}\n          requestedExposureCustomer={requestedExposureCustomer}\n          requestedExposureOutstanding={requestedExposureOutstanding}\n          requestedExposureInvoiceOutstanding={requestedExposureInvoiceOutstanding}\n          requestedExposureUnapplied={requestedExposureUnapplied}\n          tab={tab}\n          search={search}\n          partyFilter={partyFilter}\n          statusFilter={statusFilter}\n          currencyFilter={currencyFilter}\n          dueFilter={dueFilter}\n          fromDate={fromDate}\n          toDate={toDate}\n          partyOptions={partyOptions}\n          currencyOptions={currencyOptions}\n          currentRows={currentRows}\n          filteredRows={filteredRows}\n          filteredBridgeTotals={filteredBridgeTotals}\n          canManageSettlement={canManageSettlement}\n          money={money}\n          settlementActionLabel={settlementActionLabel}\n          viewAnchorLabel={viewAnchorLabel}\n          setSearch={setSearch}\n          setPartyFilter={setPartyFilter}\n          setStatusFilter={setStatusFilter}\n          setCurrencyFilter={setCurrencyFilter}\n          setDueFilter={setDueFilter}\n          setFromDate={setFromDate}\n          setToDate={setToDate}\n          onWorkspaceSideChange={(side) => updateWorkspaceQuery({ side })}\n          onExportExposure={() => setExportRequest({ kind: 'exposure' })}\n          onOpenSettlement={openSettlement}\n          onViewOrder={viewOrder}\n        />\n\n`
source = `${source.slice(0, startIndex)}${replacement}${source.slice(nextIndex)}`
source = source.replace(
  "import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../components/ui/card'",
  "import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/card'",
)

fs.writeFileSync(pagePath, source)
console.log('Extracted Settlements exposure workspace component')
