name: CLEANUP-6F Growth Batches page support

on:
  push:
    branches:
      - cleanup/growth-batch-page-support-20260831

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Extract Growth Batches page support
        run: python scripts/cleanup_growth_batch_page_support.py
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint:js
      - name: Type-check
        run: npm run typecheck
      - name: Build
        run: npm run build
      - name: Commit validated extraction and remove temporary staging files
        run: |
          rm scripts/cleanup_growth_batch_page_support.py
          rm .github/workflows/cleanup-growth-batch-page-support.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "refactor: extract Growth Batches page support"
          git push origin HEAD:cleanup/growth-batch-page-support-20260831
