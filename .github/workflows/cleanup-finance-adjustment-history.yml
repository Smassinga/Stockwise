name: CLEANUP-6E finance adjustment history

on:
  push:
    branches:
      - cleanup/finance-adjustment-history-20260831

permissions:
  contents: write

jobs:
  apply-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Extract finance adjustment history cards
        run: python scripts/cleanup_finance_adjustment_history.py
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint:js
      - name: Type-check
        run: npm run typecheck
      - name: Build
        run: npm run build
      - name: Commit validated extraction and remove temporary staging files
        shell: bash
        run: |
          rm scripts/cleanup_finance_adjustment_history.py
          rm .github/workflows/cleanup-finance-adjustment-history.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/components/finance/SalesInvoiceAdjustmentHistoryCards.tsx src/components/finance/VendorBillAdjustmentHistoryCards.tsx src/pages/SalesInvoiceDetail.tsx src/pages/VendorBillDetail.tsx scripts/cleanup_finance_adjustment_history.py .github/workflows/cleanup-finance-adjustment-history.yml
          git commit -m 'refactor: extract finance adjustment history cards'
          git push origin HEAD:cleanup/finance-adjustment-history-20260831
