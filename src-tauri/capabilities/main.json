{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for the StockWise main window to use native dialogs, open approved external targets, and access only app-scoped or user-selected files.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:allow-open",
    "fs:default",
    "fs:allow-write-text-file"
  ]
}
