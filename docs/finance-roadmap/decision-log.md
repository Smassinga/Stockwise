# Finance Roadmap Decision Log

Record durable implementation decisions here so future sessions do not re-argue settled architecture.

| Date | Decision | Why It Was Made | Follow-up |
|---|---|---|---|
| 2026-07-11 | Governed Settlement, Cash, and Bank posting is live after migration 39, frontend alignment, and controlled production smoke. | Release `5e47a9d279e4db7c4f588d420bd9439b751d260d` passed Validation `29130740318`; the hosted push applied only `20260709222842_governed_settlement_posting.sql`, and controlled cash/manual-bank/two-row atomic-import smoke proved posting, refresh, and reload-safe replay without stock, price, finance-document, or anchor duplication. | Treat the five governed RPCs as the maintained write boundary. Keep invalid-batch, repeated-`0.005`, over-settlement, mismatch, stale-anchor, cross-company, role-negative, failed-import rollback, and concurrency mutation testing local-only. The historical Forward-State package remains unprovable as a standalone rollout from current local evidence. |
| 2026-07-10 | Settlement, Cash, and Bank posting must use governed RPCs plus `posting_requests`; direct maintained frontend ledger inserts and per-row CSV commit loops are not acceptable mutation paths. | Repeated clicks, retry, stale tabs, concurrent full settlements, and interrupted imports could otherwise duplicate cash/bank rows or settle an obsolete `SO`/`PO` anchor after an `SI`/`VB` becomes the financial truth. Additive monetary tolerance also allowed repeated `0.005` attempts after outstanding had resolved to zero. | Keep `settlement.cash.post`, `settlement.bank.post`, `cash.adjustment.post`, `bank.ledger.post`, and atomic `bank.ledger.import` authenticated-only with ADMIN+ authority. Normalize requested amount and outstanding to the existing two-decimal base-currency contract, reject normalized zero outstanding exactly, and keep import identity canonical/SHA-256, reload-safe, order-insensitive, duplicate-preserving, capped, and all-or-nothing. Preserve local-only regression coverage until a separately authorised rollout. |
| 2026-04-01 | Settlement truth moves from `SO` to `SI` and from `PO` to `VB` once the finance document exists. | Orders are operational/commercial documents; issued/post finance documents are the legal/financial truth. | Preserve this in all new settlement, reminder, and reporting work. |
| 2026-04-01 | Pre-existing order-linked receipts/payments must be re-associated to the finance document when the finance document is created. | Prevent duplicate exposure and keep one active settlement anchor. | Keep validating this in future regression coverage. |
| 2026-04-03 | Sales invoice corrections support partial and cumulative credit notes. | Full-only reversal is too primitive for real AR operations. | Keep invoice state and outstanding logic cumulative. |
| 2026-04-03 | Sales invoice corrections also support debit notes and mixed credit/debit adjustment chains. | AR needs upward legal corrections, not only downward reversals. | Treat `current legal amount` as original minus credits plus debits. |
| 2026-04-03 | The lower-left invoice output card now carries `Motivo de isenção do IVA` instead of the old fiscal summary card. | VAT exemption reason is the more relevant commercial/compliance field for Mozambique output. | Keep this field explicit on issue and output flows. |
| 2026-04-04 | AP uses the same maturity model as AR where justified: vendor bills as anchors, supplier credit notes to reduce liability, supplier debit notes to increase liability. | AP was materially underbuilt compared with AR and needed coherent parity. | Keep AP parity focused on finance coherence, not blind symmetry. |
| 2026-04-04 | Supplier invoice reference and StockWise internal reference are separate concepts. | Supplier reference originates externally; the internal key is StockWise audit/system identity. | New UX must keep this distinction explicit and avoid ambiguous prefixes. |
| 2026-04-04 | Legacy `COD-*` internal references remain for audit continuity, but new internal AP references use clear prefixes such as `VB`, `VCN`, and `VDN`. | `COD` is a legacy company-derived prefix, not a finance document-type code. | Avoid introducing unexplained prefixes on new finance screens. |
| 2026-04-04 | Due reminders must be redesigned to follow the active finance anchor once an invoice exists. | Sales-order reminders contradict the settlement-anchor model after invoice issuance. | Track this in Phase 3 until implemented. |
| 2026-04-04 | Customer due reminders now follow the active AR anchor: sales order before issuance, sales invoice after issuance. | Reminder truth must follow the same legal/financial anchor as settlement truth and current legal outstanding. | Keep reminder rows anchored to invoice outstanding and invoice language snapshot when an issued invoice exists. |
| 2026-04-04 | Document language behavior remains a tracked gap. | The app supports `pt`/`en` selection and stores document language, but output helpers still render Portuguese-first. | Resolve source-of-truth and implement bilingual output in a future roadmap item. |
| 2026-04-04 | Finance-document output language is snapshot-first, with app/document language as fallback only when no snapshot exists. | Issued/post output must remain stable, while unsnapshotted documents still need to follow the currently selected language. | Keep HTML, PDF, print, and share output on the same bilingual helper and do not reintroduce Portuguese-only labels. |
| 2026-04-04 | Engineering roadmap tracking stays repo-first for now. | Current Settings and Mozambique Compliance screens are tenant-facing operational/compliance surfaces, not the right place for engineering execution status. | If in-app visibility is ever needed, add a restricted internal/admin route rather than cluttering tenant-facing UI. |
| 2026-04-04 | Phase 1 permissions use the current company role model with an approval checkpoint on base AR/AP documents. | This delivers enforceable control without inventing a second role system or premature escalation complexity. | Keep `OPERATOR+` for draft preparation and approval submission; keep `ADMIN+` for approval, legal issue/post, void, adjustment, settlement-sensitive actions, and due-reminder policy changes. |
| 2026-04-08 | Phase 2 finance audit uses a shared journal model plus UI fallback synthesis for older records. | Existing live documents predated some of the richer event triggers, but finance users still need a coherent history without rewriting historical legal documents. | Keep DB journaling authoritative for new events, but allow detail pages to synthesize missing create/approval/adjustment/settlement steps from stored timestamps and linked rows when older records lack those event rows. |
| 2026-04-08 | Parent invoice and vendor-bill histories must explicitly include related adjustment creation/posting and settlement events. | Header-only lifecycle rows are not enough for finance review; users need to see how the legal amount changed and when cash/bank activity hit the active anchor. | Preserve the parent-adjustment and settlement event triggers and keep the activity journal focused on actor, time, transition, and related-document context. |
| 2026-04-08 | Adjustment documents now require structured reason codes alongside optional narrative detail. | Free text alone is weak for audit review and reporting; finance needs consistent categories without losing operational detail. | Keep reason code mandatory on AR/AP credit/debit note flows and surface both the structured label and free-text explanation in chain and table views. |
| 2026-04-08 | Native sales-invoice drafts now auto-bootstrap the minimum Mozambique fiscal settings and current-year fiscal series when the company has not been configured yet. | The draft header trigger allocates the legal reference immediately, so draft creation was failing on `company_fiscal_settings_missing` before users could even open the fiscal invoice for eligible sales orders. | Keep draft creation tolerant of missing fiscal defaults, but keep issue-time seller tax identity and legal snapshot validation strict. |
| 2026-04-08 | Purchase-order to vendor-bill actions now follow one deterministic state model across list and detail surfaces. | The draft-creation RPC already existed, but the UI still mixed legacy status checks, missing billability explanations, and stale existing-bill handling, which left approved POs looking ambiguous or dead-ended. | Keep the VB action state derived from approved workflow, billable lines, existing bill linkage, and current permission; if a stale create attempt collides with an existing bill, refresh and open that bill instead of stopping on an error. |
| 2026-04-09 | PO billability is separate from receipt status. | The prior action state still treated active `purchase_order_lines` as the billing source, while receipt cleanup could remove fully received lines. That wrongly made physical receipt suppress AP billing. | One Vendor Bill per PO remains the current policy: if no draft/posted bill exists, an approved PO with positive purchased value remains billable even when partially or fully received; if a bill exists, open it instead of creating a duplicate. |
| 2026-04-12 | Sales-invoice issue readiness must be surfaced explicitly in the UI before calling `issue_sales_invoice_mz`. | The live issuance failure was being reported only as an RPC `P0001`, while the real blocker was hidden in issue-time legal requirements. The exact failing invoice was already approved, so `document_workflow_status = 'draft'` at issue attempt was expected and not the root cause. | Keep the readiness RPC and user-facing blocker mapping aligned with the same DB validators that govern issue-time compliance. |
| 2026-04-12 | Missing seller tax identity remains a hard Mozambique issue-time blocker and must not be auto-invented from partial company data. | The failing company had legal name and address, but `companies.tax_id` was null, so the invoice could not satisfy the required seller legal snapshot. Draft preparation may backfill from real master data, but it must not fabricate a NUIT to make the issue button work. | If a company master is incomplete, keep the invoice blocked with a precise operator-facing message until the company legal profile is completed. |
| 2026-04-12 | Approved exempt invoices may persist `vat_exemption_reason_text` through the narrow issue-preparation path without reopening to editable draft. | Approved drafts are intentionally content-locked, but exempt invoices still need a finance-safe way to freeze the statutory exemption wording before issue. Returning the document to draft just to write that one legal field is unnecessary friction. | Keep the hardening guard bypass narrowly scoped to issue-time legal snapshot fields and exemption wording only; do not allow it to change workflow or general draft content. |
| 2026-04-12 | Phase 3A reconciliation now runs from DB-backed controller views instead of duplicated frontend math. | Month-close review needs one authoritative finance bridge for AR/AP anchors, aging, and exceptions. Recomputing these per screen would drift from the live finance state. | Keep `v_finance_reconciliation_review` and `v_finance_reconciliation_exceptions` as the shared read model for Settlements and detail-page review context. |
| 2026-04-12 | The first controller review surface lives inside Settlements, not in a separate month-close route. | Finance users already go to Settlements for active exposure. Extending that workspace with a review register and exception queue delivers value faster without fragmenting navigation. | If a dedicated close workspace is added later, it should reuse the same reconciliation views rather than fork the bridge logic. |
| 2026-04-12 | Phase 3B should focus first on Items and Assembly clarity before Phase 3C adds time-based planning. | The current Items page is too flat for foundational master data, and the current Assembly page is BOM/RPC-centred rather than operation-centred. Adding time logic before clarifying those workflows would compound confusion. | Implement 3B first or in parallel with only the minimal 3C model scaffolding required by the Assembly redesign. |
| 2026-04-12 | Phase 3C assembly planning should stay lightweight and time-oriented, not full ERP scheduling. | StockWise needs practical build-time estimation and capacity guidance, not a full scheduling engine. | Add `time_per_unit`, optional setup time, available-hours planning, and stock-vs-time limiting-factor guidance without introducing full work-centre scheduling complexity. |
| 2026-04-13 | Phase 3B introduces a lightweight item-profile layer instead of a heavy new item-type subsystem. | Users needed clearer master-data meaning immediately, but the existing inventory/finance model already assumed item behavior through workflow usage. Adding `primary_role`, stock tracking, buy/sell flags, and an assembly flag gives the UI an explicit operational vocabulary without reopening post-create mutation risk. | Keep post-create editing limited to `min_stock` in normal operations. If stronger DB enforcement is added later, design it to respect legacy items and migration backfills first. |
| 2026-04-13 | The Assembly page now centers on readiness and sufficiency before build execution. | The old page exposed BOM editing and raw RPC execution more prominently than the operator’s real questions: what is being built, what will be consumed, what limits output, and whether the current plan is safe to post. | Phase 3C should extend the new planning/readiness structure with time-per-unit, setup time, available hours, and stock-vs-time limiting factors rather than rebuilding the page again. |
| 2026-04-13 | Phase 3C stores planning time on the BOM version in normalized minutes, not on the generic item master. | Different recipe revisions can have different build pace and setup effort. Keeping planning time on the BOM version preserves that operational truth while keeping the math simple and stable across UI surfaces. | Continue to expose hours/minutes in the UI, but keep storage normalized in `assembly_time_per_unit_minutes` and `setup_time_per_batch_minutes`. |
| 2026-04-13 | Available work time is an advisory planning input, not a hard execution gate. | Operators need to understand whether a plan fits inside today’s available hours, but build posting must remain governed by real stock, routing, and permission controls rather than speculative schedule data. | Surface time-limited plans clearly, keep build execution blocked only by actual stock/routing/permission failures, and do not fabricate time estimates when a recipe has no planning data. |
| 2026-04-13 | Bank settlement posting must resolve through `bank_accounts`; `public.banks` is obsolete in the live schema. | The bank receive/pay failure was caused by a stale `public.banks` lookup inside the settlement-event journal trigger. The rest of the active bank posting model already uses `bank_accounts`, so reviving a fake compatibility table would have hidden schema drift instead of fixing it. | Keep bank posting, read models, and future treasury work anchored to `bank_accounts`, and treat any remaining `public.banks` reference as drift to remove rather than preserve. |
| 2026-04-13 | Banks, Cash, and UOM should use page language that matches the real operational model, not vague or overly technical labels. | Users were being asked to operate on bank accounts, a company cash book, and a split unit-master/conversion model, but the page structure and wording did not make those distinctions obvious. That increased friction and error risk on finance-adjacent setup screens. | Keep future treasury and master-data UX honest to the underlying model: bank-account register, cash-book ledger, and global units plus company conversion rules. |
| 2026-04-16 | Automated finance regression coverage now runs as a real mutation suite through the Node test runner and live Supabase clients. | The finance platform had become too broad for manual-only regression checking. High-value flows needed repeatable protection over state transitions, anchor math, and permission-sensitive posting paths. | Keep `npm run test:finance-regression` green after finance, treasury, inventory, assembly, and access-control changes; next follow-up is CI wiring with guarded mutation rules. |
| 2026-04-16 | Payment automation remains intentionally deferred; manual plan activation is the live commercial model. | StockWise now needs commercial control and auditability before self-serve billing. Public pricing can be visible in MZN while entitlements remain manually granted by internal admins. | Future payment gateway work must reuse `plan_catalog`, `company_subscription_state`, and the existing access-audit trail instead of redesigning tenant entitlement state. |
| 2026-04-16 | Trial expiry must preserve credentials but may schedule purge of company operational data. | Expired trials need to be enforceable without destroying user identity. Keeping credentials intact allows later reactivation while still enabling operational-data cleanup. | Retain auth credentials and access audit history. Purge scope targets company operational data only, and automatic destructive execution remains future scope. |
| 2026-04-16 | Entitlement enforcement belongs in backend access helpers first, with app routing as a mirror. | Restricting expired or suspended tenants in the UI alone would be cosmetic and unsafe. Membership and authentication are not enough when tenant entitlement state can disable normal use. | Keep `company_access_effective_status`, `company_access_is_enabled`, and related helper replacements authoritative; frontend route guards should only reflect the same backend truth. |
| 2026-04-16 | Public bootstrap abuse protection starts with backend rate limiting on tenant creation. | Trial signup was the most obvious public abuse surface in the current commercial model. A first-layer throttle materially reduces repeated bot-driven or accidental rapid bootstrap attempts without blocking future payment design. | Keep the company-bootstrap rate limit in place and extend throttling only where live abuse or operational disruption shows real need. |
| 2026-04-16 | Security tightening must preserve operational notification writes. | Access and RLS hardening exposed a real continuity gap when authenticated company-scoped actions attempted to write `notifications` rows and hit RLS failure. Tightening without continuity coverage would have broken legitimate operational workflows. | Keep the scoped `notifications` insert policy aligned with operator-plus company access, and let the regression suite continue protecting that path. |
| 2026-04-17 | Platform admin access stays permission-based and discoverable through runbook plus navigation, not a fake UI toggle. | Users needed a clear way to reach the control plane, but exposing “admin mode” to everyone would weaken the security model. The right answer is explicit bootstrap instructions, a fixed route, and navigation only for active platform admins. | Keep `/platform-control` as the canonical route, keep `platform_admins` as the authority, and document the bootstrap command in repo runbooks. |
| 2026-04-17 | The approved StockWise logo now becomes the canonical runtime brand through PNG-derived assets, while SVG extraction stays deferred. | The app still carried stale icons and mismatched brand assets. The approved artwork package included a strong raster source, but no trustworthy vector conversion tooling was available in-session. Shipping a bad trace would have been worse than standardizing on high-quality PNG assets. | Keep the current PNG-derived web/PWA/Tauri assets as canonical runtime brand outputs, and only add SVG later if the approved source can be extracted cleanly. |
| 2026-04-17 | Public pricing now follows the 2026 MZN workbook package structure and remains manual-activation-first. | Pricing needed to look commercially credible and locally grounded without implying self-serve payment flows that do not exist yet. The workbook already defined the plan structure, perks, and onboarding posture. | Keep the pricing section titled `Pricing`, keep MZN in the values rather than the heading, and preserve manual activation / no-checkout messaging until payment automation is actually implemented. |
| 2026-04-17 | Dead demo/default brand assets and unfinished public copy should be removed instead of left dormant in the repo-facing runtime surface. | Even unused leftover demo assets and generic placeholder copy make the product feel unfinished and create future ambiguity about the real brand/control-plane posture. | Keep public/app copy professional, remove stale runtime assets when superseded, and treat placeholder cleanup as product work rather than optional polish. |
| 2026-04-18 | Platform Control status edits must bind to the selected company detail snapshot, not to a list row plus an unstable render-local helper. | The status dropdown bug was caused by a `useEffect` reset keyed on an inline `tt(...)` function. That function changed on every render, so the form kept reloading `selectedRow.subscription_status` and visually pinned the control to `active_paid` even though the backend mutation path itself was valid. | Keep the selected-company editor driven by a dedicated detail fetch and stable effect dependencies; do not reintroduce render-local functions into form-reset effects. |
| 2026-04-18 | Company owner and last-login metadata must come from explicit source order rather than guessed display fallbacks. | Platform admins asked for trustworthy company intelligence. Falling back to a company legal name as an “owner” would have created fake metadata. The canonical owner now resolves from `companies.owner_user_id`, then active `OWNER`, then active `ADMIN`, while sign-in activity uses `profiles.last_sign_in_at` when present. | Keep owner provenance explicit in the UI and show “not captured” when profile activity is unavailable. |
| 2026-04-18 | Company operational reset is allowed only for non-active-paid tenants and must preserve identity and control-plane records. | A reckless delete button would have been unsafe, but platform admins still need a way to clear trial/demo tenant data. The implemented reset uses UUID confirmation, mandatory reason, audit logging, rate limiting, and the existing privileged finance-transition bypass so finance-immutable data can be removed only inside the guarded admin RPC. | Keep reset scope limited to company operational data, preserve auth credentials and subscription/access records, and block reset while a tenant is in active paid access. |
| 2026-04-18 | Platform Control summary cards now track each effective status explicitly instead of combining suspended and disabled under one partial count. | The summary reducer already used `effective_status`, but the UI labeled one card “Suspended / disabled” while only reading `summary['suspended']`. That made disabled companies disappear from the top-line admin view even though the register/detail data was correct. | Keep summary counters derived from the same effective-status source as the company register and render suspended/disabled as separate cards unless a combined aggregation is intentionally coded. |
| 2026-04-18 | Inbound support routing and outbound company-access notifications must stay separate. | Users need one clear inbox for activation/support requests, but outbound renewal/expiry/purge/activation notices must go to the selected company, not back to StockWise itself. Mixing those paths would create operational confusion and break recipient expectations. | Keep inbound activation/support CTAs on `geral@stockwiseapp.com`; keep outbound access emails on the canonical company recipient with support only as the reply-to/contact target. |
| 2026-04-18 | Canonical outbound company-access recipient resolution is `companies.email` first, then owner email, then active admin email fallback. | Platform Control needed a real recipient rule, not a guessed frontend fallback. The company record is the clearest registered contact when present; otherwise the resolved owner/admin chain already exists in the control plane. | Keep recipient provenance visible in Platform Control and block preview/send when no safe company recipient can be resolved. |
| 2026-04-18 | Company access emails are manual preview/send actions, not automatic side effects of status mutation. | Manual activation remains the commercial model. Sending expiry, purge, or activation emails automatically inside the status mutation path would be harder to review, harder to audit, and easier to misfire when admins are still editing dates. | Keep preview/send explicit in Platform Control, use stored access state rather than unsaved form edits, and audit successful sends in `company_control_action_log`. |
| 2026-04-19 | Android/mobile UX adaptation is a general shell and page-structure concern, not the same feature as the Operator workflow or onboarding/import. | Product direction was clarified: mobile friendliness improves the app overall, while the Operator page and onboarding import solve different business problems. Blending them would make roadmap tracking and future prioritization vague. | Keep Phase 6 split explicitly into workstreams A, B, and C in code, docs, and future planning. |
| 2026-04-19 | The Operator page defaults to the walk-in/cash customer model and should not force named-customer capture for common small-store sales. | The primary use case is replacing handwritten issue books in small stores with fast stock-out entry. Most daily counter sales are to unknown walk-in buyers, so requiring formal customer setup would recreate the friction the page is meant to remove. | Keep named customer selection optional, preserve the default cash customer path, and avoid turning the Operator page into a full ERP sales-order form. |
| 2026-04-19 | Operator posting now uses a dedicated backend batch issue path that creates a shipped sales order plus stock movements in one operation. | The Operator workflow needed speed and stock correctness without forcing users back through the heavier order screens. A dedicated RPC can validate stock, create/reuse the walk-in cash customer, and keep the resulting issue auditable through normal sales-order anchors. | Keep the path simple-sale oriented, maintain shipped-state and stock-reduction assertions in regression coverage, and extend it only when a concrete operational need appears. |
| 2026-04-19 | Onboarding/import in this block is intentionally limited to opening/master data, not historical document migration. | Existing businesses need a practical way to start using StockWise from today forward. Importing current items, counterparties, locations, and on-hand stock delivers that value without the legal/audit complexity of pretending to recreate historical SO/PO/invoice/vendor-bill history inside StockWise. | Keep import messaging explicit about current/opening data, require preview/validation before commit, and treat historical document migration as separate future scope if ever needed. |
| 2026-04-19 | Opening stock import must use a server-authoritative batch path instead of raw client-side movement inserts. | The first onboarding pass exposed a real continuity gap: a receive movement could be recorded for a new go-live item without leaving a usable `stock_levels` bucket. That breaks day-one usability even though the audit row exists. | Keep opening stock import on the dedicated RPC, preserve the receive audit rows, and let regression coverage protect both new-bucket creation and existing-bucket weighted-cost updates. |
| 2026-04-19 | The legacy Supabase migration chain was retired and replaced with a canonical baseline reset. | Fresh replay failed immediately because the earliest tracked migration referenced `public.companies` before any tracked migration created it. The repo did not actually contain a trustworthy bootstrap schema, and the linked project only contained disposable test data. Preserving that chain would have kept a structurally invalid history alive. | Treat `20260419142000` + `20260419143000` + `20260419144000` plus `supabase/roles.sql` as the new canonical replay base. Keep archived legacy files only as reference, not as active migration history. See `docs/CANONICAL_MIGRATION_BASELINE_2026-04-19.md`. |
| 2026-04-19 | Synthetic `*_remote_schema.sql` files from `db pull` are review artifacts, not canonical migrations. | After the canonical baseline reset, `db pull` could still emit a synthetic remote schema file and even mark it applied remotely. Treating that as part of the live chain would pollute the new baseline immediately. | Keep `npm run check:migrations`, ignore these files by default, and revert any accidental remote-history application unless the file is explicitly reviewed and accepted as a real schema delta. |
| 2026-04-19 | The first post-baseline schema cleanup removes `user_profiles`, `company_role`, and `movements` instead of preserving duplicate concepts. | These objects overlapped directly with the real current models: `profiles` + `user_active_company`, `member_role`, and `stock_movements`. The project only holds disposable test-era data, so preserving the duplicates would keep future sessions confused for no real business benefit. | Keep one canonical concept per responsibility and document the cleanup in `docs/DATA_MODEL.md` and `docs/DEVELOPMENT.md`. |
| 2026-04-19 | Point of Sale must prefill from `items.unit_price`, not from weighted-average cost or stock valuation. | Counter-sale workflows need a commercial selling default, not an inventory valuation proxy. Using cost as the default sell price would be both operationally wrong and commercially unsafe. | Keep sell price on the item master for sellable roles, prefill Point of Sale from that field, and still allow operator-side line-price edits before posting. |
| 2026-04-19 | Current-truth docs and Tauri release notes should be smaller and more specific, not broader and more generic. | After the canonical baseline reset and Phase 6 work, the repo risk shifted from missing capability to repo drift: generic API/component brochures, stale mobile summaries, and older Tauri notes were more misleading than helpful. | Keep one maintained document per topic, remove dead helper files and bootstrap-era scripts when they are truly unused, and make Tauri desktop/Android docs reflect the current StockWise shell, branding, Point of Sale naming, and onboarding/import reality. |
| 2026-04-23 | The authenticated shell should adapt by page class and screen size instead of forcing one narrow max-width everywhere. | The previous polish pass improved surface quality, but live review still showed the same app canvas behaving too narrowly on some wide screens and too rigidly on some compact ones. The correct fix is deliberate page-width variants plus breakpoint-specific shell spacing, not simply stretching everything. | Keep page-width variants such as dashboard/analytics versus workspace layouts, keep readable text blocks locally constrained, and preserve a clearly separated mobile bottom dock. |
| 2026-04-23 | Notifications should expand through `finance_document_events`, but only for high-signal milestones. | StockWise already had a working notification feed, but the most useful finance lifecycle milestones were still missing. Re-implementing notification logic in many workflow screens would create drift and noise. | Promote approval-request and issue/post events from `finance_document_events` into `public.notifications`, keep the feed company-scoped, and avoid notifying on low-value draft churn. |
| 2026-04-24 | Platform-level subscription analytics belongs inside `/platform-control` and must stay admin-only, catalogue-backed, and payment-agnostic. | Platform admins need a SaaS-style portfolio view over company plan/status health, but normal tenant users must not see platform subscription data and the current system still has no payment gateway truth. The correct implementation is a protected analytics read model over `company_subscription_state` and `plan_catalog`, not a faux billing dashboard. | Keep the analytics RPC server-protected by `is_platform_admin()`, surface only catalogue-based MRR/ARR indicators, and do not represent them as collected revenue until real billing/payment data exists. |
| 2026-06-10 | Production & Costing starts with a Phase 1 Recipes & Assemblies UX pass, not new posting logic. | The current BOM/assembly foundation can support clearer recipe and material-cost planning, but it is still simple weighted-average stock transformation. Premature Production Runs, frozen snapshots, extra-cost allocation, or Growth Batches would create accounting and regression risk before the backend model is ready. | Preserve existing `build_from_bom` posting semantics, keep material cost labelled as an estimate, and require separate backend review before relying on `build_from_bom_sources`, `inv_issue_component`, or `inv_receive_finished` for future Production & Costing phases. |
| 2026-06-11 | Phase A1 hardens current assembly RPC authority and build audit linkage before adding Production Runs. | The source-split/helper path was weaker than the simple build path, and build movements were not linked back to the generated build id. Hardening the existing RPCs reduces immediate assembly risk without introducing production-run accounting, POS pricing changes, finance posting, or growth-batch scope. | Keep `stock_movements` as the ledger and `stock_levels` as the derived rollup. A2 must add idempotency, repeated-click replay, and concurrent stock-decrement safety before Production Runs depend on this foundation. |
| 2026-06-11 | Opening-stock import keeps UOM identifiers as canonical text IDs. | Local A1 validation exposed that `import_opening_stock_batch` validated UOM rows as text but staged them as UUID, which rejected canonical IDs such as `uom_ea`. The schema truth is text UOM IDs across `uoms.id`, `items.base_uom_id`, and `stock_movements.uom_id`. | Preserve text UOM IDs in opening-stock import. Do not convert the UOM catalogue to UUID, and keep this unblocker separate from Production Runs, Growth Batches, POS pricing, invoice, settlement, entitlement, Platform Control, and subscription behavior. |
| 2026-06-11 | A2.1/A2.2 adds a generic posting request ledger and applies idempotency to assembly only. | Duplicate assembly submits could create duplicate builds and stock movements if stock remained available. A company-scoped `posting_requests` ledger gives StockWise a reusable backend idempotency foundation while limiting the first rollout to the already-hardened assembly RPCs. | Use `post_build_from_bom` and `post_build_from_bom_sources` for `/bom`. Keep existing RPCs compatible. A2.3 must still harden concurrent stock decrement in `apply_stock_delta`, and later phases must extend idempotency to POS, PO receiving, sales shipping, opening-stock import, and manual movements. |
| 2026-06-13 | A2.3 hardens shared stock rollup concurrency without changing valuation policy. | `apply_stock_delta` previously made unlocked read-then-update decisions, so concurrent issue and receipt inserts could pass stale availability checks or lose weighted-average updates. Production Runs need this stock-engine foundation before they add more posting volume. | Negative deltas now use an atomic guarded update, receipts use nullable-bin aware upserts, and regression coverage includes assembly race, receipt race, ledger reconciliation, and POS-versus-assembly competition. A2.4 must still move remaining direct client posting workflows behind backend RPCs and extend idempotency beyond assembly. |
| 2026-06-13 | A1-A2.3 is live and production-smoke validated. | The hosted migrations were applied and the already-deployed frontend was aligned with the backend. A controlled `/bom` assembly smoke created exactly one build, seven component issues, and one finished receipt, with BUILD-linked movements, reconciled stock rollups, expected weighted-average cost, unchanged `items.unit_price`, zero duplicate stock buckets, and no relevant Postgres errors in the smoke window. | Treat A2.3 as complete. The closeout success-feedback patch is UI-only. Do not run the full finance regression suite against production. A2.4 is the next backend-hardening package and must extend governed, idempotent posting beyond assembly before Production Runs. |
| 2026-06-13 | A2.4a.1 applies the posting request idempotency pattern to normal web Point of Sale while leaving legacy RPCs temporarily executable. | POS already had a backend transaction boundary, but repeated requests could duplicate the sale, stock issue, and cash/bank settlement. Wrapping the existing sale-and-settlement RPC preserves current commercial pricing and settlement behavior while adding replay safety for the web client. Legacy execution stays available for deployment compatibility and stale Tauri clients until a separate closure package reviews distribution posture. | Use `post_operator_sale` with operation type `operator.sale` for normal web POS after the hosted migration and frontend rollout. Same-key/same-payload replay returns the original sale result; same-key/changed-payload is rejected. A2.4a.2 must revoke normal legacy POS RPC access after rollout verification, and A2.4b must continue with PO receipt authority/idempotency. |
| 2026-06-14 | A2.4a.1 normal web POS idempotency is live and production-smoke validated. | The database-first rollout applied `post_operator_sale`, the production frontend deployed the web POS cutover, and a controlled cash-sale smoke created one sales order, one line, one issue movement, one cash transaction, and one succeeded `operator.sale` posting request without duplicate rows, negative stock, or pricing drift. Backend replay is now governed by `posting_requests` for normal web POS. | Keep legacy POS RPC execution temporarily available for stale Tauri compatibility until A2.4a.2. Do not claim full POS authority closure yet. A2.4b should continue with PO receipt authority/idempotency before Production Runs. |
| 2026-06-14 | The consolidated A2.4/A2.5 local package moves remaining maintained stock-posting workflows behind governed idempotent RPCs. | PO receiving, sales shipping, opening-stock import, and manual receipt/issue/transfer/adjustment still had maintained paths that could duplicate stock or document progress on replay. Dedicated workflow RPCs preserve domain validation while reusing `posting_requests`, the append-only `stock_movements` ledger, and the trigger-derived `stock_levels` rollup. | Use `purchase.receive`, `sales.ship`, `opening_stock.import`, `stock.receipt`, `stock.issue`, `stock.transfer`, and `stock.adjustment` as operation types. Keep A2.4a.2 legacy POS closure separate. Production Runs may start only after this consolidated package is reviewed, deployed, and production-smoke validated. |
| 2026-06-14 | Consolidated A2.4/A2.5 governed stock posting is live and representative production-smoke validated. | The hosted migration chain is aligned through `20260614123300`, the production frontend is commit `51c4fd1`, and controlled smokes passed for PO receipt, sales shipment, transfer, and positive adjustment. The transfer smoke created one `stock.transfer` request with two balanced movements; the shipment smoke created one `sales.ship` request and one issue movement for `LEN-SO000000002`; duplicate and negative stock checks stayed zero, and `items.unit_price` stayed separate from stock cost. | Treat A2.4/A2.5 as closed for Production Runs readiness. Keep replay/mismatch testing in the local `24/24` regression suite rather than production. A2.4a.2 remains deferred for stale Tauri and legacy POS RPC compatibility. |
| 2026-06-15 | Production Runs use their own planned-versus-actual model instead of extending quick assembly rows. | Quick assembly answers the simple build question, but real production needs draft planning, actual consumed inputs, source buckets, frozen weighted-average input costs, additional direct costs, output unit cost, movement links, and controlled reversal. Cramming those concerns into `builds` would blur audit history and make reversal semantics unsafe. | Add `production_runs`, line/snapshot tables, `post_production_run` (`production.run.post`), and `reverse_production_run` (`production.run.reverse`) locally. Keep `/bom` quick assembly available. Treat extra costs as production-cost snapshots, not finance postings. Growth Batches remain next after Production Runs rollout and smoke validation. |
| 2026-06-18 | Production Run mutation is RPC-only and first-release quantities are base-UOM-only. | Pre-staging review found that direct authenticated table mutation and implicit UOM assumptions could weaken the planned-versus-actual production model. The first release should be explicit: clients may read permitted rows, but create/edit/cancel/post/reverse must go through maintained RPCs, and BOM input/output quantities are recorded in each item base UOM. | Revoke authenticated table mutation grants, retain company-scoped read policies, add structural validation triggers, keep `production.run.post` and `production.run.reverse` idempotent, defer generic Production Run UOM conversion to a later design phase, and require fresh preview plus exact run-reference confirmation in the UI. |
| 2026-06-18 | Production Runs is live and production-smoke validated. | The database-first rollout applied the Production Run migrations, the production frontend deployed commit `4f82c5a`, and a controlled `Leny Doçuras` smoke posted and immediately reversed Production Run `LEN-PR000000001`. Posting created one `production.run.post` request, seven input issues, and one output receipt; reversal created one `production.run.reverse` request, one compensating output issue, and seven compensating input receipts. | Treat the first Production Runs package as closed. Keep production replay/mismatch coverage in the local `26/26` regression suite, keep additional direct costs as memo production-cost snapshots with no finance postings, keep A2.4a.2 legacy POS closure separate, and begin Growth Batches as the next roadmap phase. |
| 2026-06-19 | Growth Batches G1-G2 starts with a local RPC-only group-batch lifecycle, not stock/finance valuation. | Biological and agricultural batches need draft setup, activation, measurements, and memo direct-cost rollups before StockWise can safely model harvest, mortality, transfers, fair value, FIFO, COGS, or finance posting. Collapsing those into the first package would imply a live costing policy that is not implemented end to end. | Keep G1-G2 local until an approved hosted DB push succeeds. Use Growth Batch RPCs and `growth.batch.*` request types for mutation, keep authenticated table mutation blocked, keep direct costs memo-only, keep `items.unit_price` commercial, and defer stock inputs, harvest/completion/reversal, FIFO, COGS, and finance posting to explicit later phases. |
| 2026-06-20 | Growth Batch weight values must carry a batch-linked UOM and event histories must be explicitly ordered. | The first inspection found that optional numeric idempotency hashes could collapse null/zero semantics, weight measurements could become ambiguous without a stable batch weight unit, and view row order should not be treated as guaranteed. | Keep structured JSON payload hashes with explicit presence markers, require configured batch UOMs for total/average weight and area observations, allow negative temperature only, enforce start-date/future-date chronology guards, and have callers order histories by exposed event sequence/effective timestamp/id fields. |
| 2026-06-20 | Growth Batches use a group-level operational model rather than per-animal or per-plant inventory records. | G1-G2 needs a practical farm/production batch ledger before StockWise introduces biological inventory layers, mortality, transfers, harvest, or COGS. Per-unit biological records would overfit the first release and imply valuation detail that is not live. | Keep `/growth-batches` focused on group batch draft/edit, activation, measurements, memo direct costs, and read-model review. Treat per-animal/per-plant tracking, stock-input consumption, harvest, and biological valuation as separate future phases. |
| 2026-06-20 | G1-G2 direct costs are memo costing only and do not post to stock or finance. | Controlled production smoke verified one Water memo cost updated Growth Batch rollups while stock movements, stock levels, cash, bank, vendor bills, invoices, finance document events, and `items.unit_price` stayed unchanged. | Keep Growth Batch direct costs out of AP, cash/bank settlement, invoices, journals, COGS, and item commercial pricing until a later reviewed finance/allocation package explicitly changes that boundary. |
| 2026-06-20 | Weight observations use a frozen batch weight UOM without automatic conversion. | Weight values are only meaningful when their UOM travels with the batch and read models. G1-G2 can enforce canonical text UOM IDs and batch-linked total/average weight without taking on conversion tables or ambiguous reinterpretation. | Preserve `weight_uom_id`, display every total/average weight with UOM, reject mismatched weight measurements, and defer conversion to a later explicit UOM design. |
| 2026-06-20 | Growth Batch events are append-only and backend-idempotent. | Draft, activation, measurement, and direct-cost flows must survive repeated submissions without duplicate events/details or rollup drift. The smoke verified succeeded posting requests and one activation/measurement/cost event; local `31/31` regression covers replay, mismatch, concurrency, and failure paths. | Keep `growth.batch.create`, `growth.batch.activate`, `growth.batch.cancel`, `growth.batch.measurement`, and `growth.batch.cost` on `posting_requests`, preserve immutable event rows, and keep histories ordered by event sequence. |
| 2026-06-20 | Growth Batches G1-G2 is live after controlled UI smoke; G3-G5 remain separate phases. | The database-first rollout applied the two Growth Batch migrations, production deployment `dpl_3ouAxVTpzLpAek6GGSMjP6hQ5pbR` served commit `c7b5e299`, and controlled smoke retained active batch `LEN-GB000000001` with reconciled read models and unchanged stock/finance/price anchors. | Treat G1-G2 rollout as closed after documentation and CI. Do not begin stock inputs, mortality, transfers, harvest, completion, reversal, FIFO, COGS, fair value, automatic finance posting, or profitability dashboards without separate G3+ design and review. |
| 2026-06-20 | Growth Batches G3 stock inputs use physical stock movements and frozen WAC material cost, not finance posting. | G3 needs real inventory consumption before harvest or COGS can be designed. The safe boundary is event-specific stock input for active batches: issue stock from source buckets, freeze source WAC on immutable detail rows, recalculate Growth Batch material cost, and allow MANAGER+ compensating receipt reversal without editing original issue movements. | Before hosted rollout, keep G3 local and require independent inspection and authenticated visual QA; after rollout, use `growth.batch.input` and `growth.batch.input.reverse` request types, event-specific movement references, base-UOM-only consumed lines, immutable input/reversal details, and no cash, bank, vendor bill, supplier liability, settlement, invoice, finance journal/event, `items.unit_price`, FIFO, fair value, COGS, harvest, transfer, mortality, or whole-batch reversal behavior. |
| 2026-06-21 | Growth Batches G3 completed local validation before hosted rollout. | Local replay of 30 migrations passed, Growth Batches regression passed `5/5`, complete finance regression passed `31/31`, independent inspection passed, and authenticated local visual QA passed at `1440`, `1200`, `820`, and `390` in light and dark mode using isolated local batch `GVI-GB000000001`. The smoke verified preview, stale-preview protection, duplicate and insufficient-stock blockers, OPERATOR+ posting, frozen WAC `MZN 2.50`, material cost `MZN 12.50`, MANAGER+ event-specific reversal, cost restoration to `MZN 0.00`, and stock restoration to `100 EA at MZN 2.50 WAC`. | This entry is superseded by the 2026-06-22 live rollout entry. Keep production replay, mismatch, concurrency, and failure-path coverage in guarded local validation rather than production smoke. |
| 2026-06-22 | Growth Batches G3 is live after database-first rollout and controlled UI smoke. | The hosted rollout applied `20260620132646_add_growth_batch_stock_inputs.sql` and `20260620132656_add_growth_batch_stock_input_posting.sql`, bringing production to 30 migrations. Vercel deployment `dpl_CPHfKuoWcZ1eEMLrFXjv3cSFCu3i` served commit `58e8a083`, and Validation run `27930016751` passed. Controlled smoke used tenant `Leny Doçuras`, batch `LEN-GB000000002`, item `OV002 - Ovo`, and `1 EA` from `WH001 - Casa / CDC001 - Cozinha - Casa`; input event `LEN-GB000000002-E000002` created issue movement `3fe172dd-adc5-44e5-8ec6-7587420078fa`, and reversal event `LEN-GB000000002-E000003` created receipt movement `48ce328c-fdc9-4383-a0d5-11164fb0da7f`. | Treat G3 stock-input posting/reversal as live. Source stock and material cost restored, the original issue remained immutable, negative/duplicate bucket checks stayed zero, finance rows and `items.unit_price` stayed unchanged, and G4/G5 scope remains separate: mortality, transfers, harvest, completion, whole-batch reversal, FIFO, COGS, fair value, automatic finance posting, and profitability reporting. |
| 2026-06-27 | Growth Batches G4.1 implements local mortality and shrinkage without stock, cost write-off, or finance posting. | Operational loss recording needs to reduce current group quantity and/or latest total weight before transfer and harvest design, but it must not imply biological valuation, COGS, FIFO, fair-value accounting, or finance journals. The safe local package adds immutable loss and reversal detail tables, loss history, OPERATOR+ preview/record RPCs, MANAGER+ event-specific reversal, dependency blockers, and request-key idempotency while hosted production remains at the G3 migration checkpoint. | Keep G4.1 local-only until an approved hosted rollout applies `20260627225400_add_growth_batch_losses.sql` and `20260627225414_add_growth_batch_loss_posting.sql`. Use `growth.batch.mortality`, `growth.batch.shrinkage`, `growth.batch.mortality.reverse`, and `growth.batch.shrinkage.reverse` as operation types. Do not create stock movements, update `stock_levels`, write off accumulated cost, post finance rows, change `items.unit_price`, or market transfer/harvest/completion/G5 features as live. |
| 2026-06-28 | Growth Batches G4.1 is live after database-first rollout and controlled UI smoke. | The hosted rollout applied `20260627225400_add_growth_batch_losses.sql` and `20260627225414_add_growth_batch_loss_posting.sql`, bringing hosted and local history to 32 migrations. Vercel deployment `dpl_FrC2WKJsF1DmosBSu68tahEBhmhU` served commit `5a24eb428499d126870883bb5841e3e451cdd178`, and Validation run `28319500331` passed. Controlled smoke used tenant `Leny Doçuras`, batch `LEN-GB000000003`, mortality event `LEN-GB000000003-E000002`, mortality reversal `LEN-GB000000003-E000003`, shrinkage event `LEN-GB000000003-E000004`, and shrinkage reversal `LEN-GB000000003-E000005`; quantity restored to `20 EA` and weight restored to `40 KG`. | Treat G4.1 mortality/shrinkage preview, recording, loss history, and event-specific reversal as live. Loss events reduce only current quantity/latest weight, create no stock movement, do not update `stock_levels`, do not write off accumulated cost, do not post finance rows, and do not change `items.unit_price`. Keep G4.2 transfers, harvest/split outputs, completion, whole-batch reversal, FIFO, COGS, fair value, finance journals, profitability dashboards, and individual animal/plant identity future. |
| 2026-06-30 | Growth Batches G4.2 implements local full-batch operational location transfer only. | The local package adds `growth_batch_transfers`, `growth_batch_transfer_reversal_lines`, `growth_batch_transfer_history`, `preview_growth_batch_transfer`, `transfer_growth_batch`, and `reverse_growth_batch_transfer`, bringing local replay to 34 migrations through `20260630170735_add_growth_batch_transfer_posting.sql`. Hosted production remained at 32 migrations through G4.1 at this checkpoint. Targeted Growth Batch regression passed locally with transfer preview, stale-source fingerprint protection, idempotency, OPERATOR+ transfer, MANAGER+ reversal, concurrency, immutability, and stock/finance/cost/price isolation coverage. | Superseded by the 2026-07-02 live rollout entry below. Transfers are full-batch only and change only current operational location fields; they create no stock movements, do not update `stock_levels`, do not split batches, do not post transport cost automatically, do not alter Growth Batch costs or `items.unit_price`, and do not introduce harvest, completion, FIFO, COGS, fair value, finance journals, dashboards, or individual animal/plant identity. |
| 2026-07-02 | Growth Batches G4.2 is live after database-first rollout, corrective layout fix, and controlled UI smoke. | Hosted and local history are aligned to 34 migrations through `20260630170735_add_growth_batch_transfer_posting.sql`. Release commit `6995c1c59e4399258ab663953b0a129f606b92b5` passed Validation run `28606395112`; the initial UI transfer on `LEN-GB000000003` succeeded but the maintained UI reversal was blocked by a cramped detail-card/action layout. The batch was restored through the approved authenticated public `reverse_growth_batch_transfer` RPC, layout fix commit `c84469100249188144cb6305a634e21fba77a653` passed Validation run `28617062013` and deployed as `dpl_ECTTdBiBpL6y4kkm39XmsqtpmY3p`, and a fresh maintained-UI transfer/reversal completed with events `LEN-GB000000003-E000008` and `LEN-GB000000003-E000009`. | Treat G4.2 transfer preview, full-batch operational transfer, transfer history, and event-specific transfer reversal as live. Transfers update only location/audit/latest-sequence fields, create no stock movement, do not update `stock_levels`, do not alter quantity, weight, costs, finance rows, or `items.unit_price`, and do not introduce partial transfer, split/child batches, harvest, completion, FIFO, COGS, fair value, finance journals, dashboards, or individual animal/plant identity. Keep replay, mismatch, authority-negative, and concurrency tests in guarded local regression, not production. |
| 2026-07-02 | Growth Batches G5.1 implements local governed depleting harvest and event-specific harvest reversal. | G5.1 needs to move harvested biological/agricultural output into stock without implying sale, COGS, FIFO, fair value, or finance posting. The safe local boundary adds `growth_batch_harvests`, `growth_batch_harvest_reversal_lines`, `growth_batch_harvest_history`, OPERATOR+ preview/post RPCs, MANAGER+ reversal, one stock receipt per harvest, one compensating stock issue per reversal, proportional remaining-cost transfer for partial harvests, exact remaining-cost transfer for full harvests, and an active fully-harvested awaiting-completion state. Hosted production remained at 34 migrations through G4.2 at this implementation checkpoint; local replay reached 36 migrations through `20260702205834_add_growth_batch_harvest_posting.sql`. | Superseded by the 2026-07-03 live rollout entry below. Use `growth.batch.harvest` and `growth.batch.harvest.reverse`. Do not introduce non-depleting milk/egg yield, split/child batches, multi-output or co-product allocation, sales, invoices, COGS, FIFO, fair value, finance journals, automatic completion, whole-batch reversal, profitability dashboards, individual animal/plant tracking, or `items.unit_price` changes. |
| 2026-07-03 | Growth Batches G5.1 is live after database-first rollout and controlled UI smoke. | Hosted and local history are aligned to 36 migrations through `20260702205834_add_growth_batch_harvest_posting.sql`. Release commit `6f050745a9e1e5f9a56bfee7f30bca2b7ff55e10` passed Validation run `28657058435`, production deployment `dpl_4sYA2iZ1r61iB1mdZTgZxY7DPPaH` served the frontend, and the rollout created one controlled QA item `QA-G51-POULTRY-KG`. Maintained-UI partial harvest/reversal created events `LEN-GB000000003-E000010` and `LEN-GB000000003-E000011`; maintained-UI full harvest/reversal created `LEN-GB000000003-E000012` and `LEN-GB000000003-E000013`. Final state restored `20 EA`, `40 KG`, active status, zero costs, and a zero QA output bucket; finance/sales rows and pre-existing selling prices were unchanged. | Treat governed depleting harvest preview/post, one-output stock receipt, harvest history, and event-specific harvest reversal as live. Keep nonzero proportional-cost allocation, replay, mismatch, authority-negative, insufficient-stock, and concurrency coverage in local regression, not production. Completion, split/child batches, non-depleting yield, multi-output/co-product allocation, FIFO, COGS, fair value, finance journals, profitability dashboards, and individual animal/plant tracking remain future scope. |
| 2026-07-04 | Growth Batches G5.2 implements local lifecycle completion and event-specific completion reversal. | G5.2 closes the lifecycle after G5.1 full harvest without introducing stock, costing, or finance behavior. The local package adds `growth_batch_completions`, `growth_batch_completion_reversal_lines`, `growth_batch_completion_history`, `preview_growth_batch_completion`, `complete_growth_batch`, and `reverse_growth_batch_completion`, bringing local replay to 38 migrations through `20260704041943_add_growth_batch_completion_posting.sql` while hosted production remained at 36 migrations through G5.1 at this checkpoint. | Superseded by the 2026-07-09 live-rollout entry below. Use `growth.batch.complete` and `growth.batch.complete.reverse`. Completion requires active status, zero current quantity, zero current weight where weight exists, and zero remaining cost; reversal restores active status only. Do not create stock movements, update `stock_levels`, change quantity, weight, costs, `items.unit_price`, or introduce split/child batches, whole-batch reversal, FIFO, COGS, fair value, finance journals, profitability dashboards, or individual animal/plant tracking. |
| 2026-07-09 | Growth Batches G5.2 is live after database-first rollout, maintained-UI smoke, and targeted localization correction. | The authorised rollout applied `20260704041936_add_growth_batch_completion.sql` and `20260704041943_add_growth_batch_completion_posting.sql`, aligning hosted and local history at 38 migrations. Feature commit `6fa6bdb1303c9457f0b26fa6934a3d096cdad38b` passed Validation run `28706577810`. Controlled Leny Doçuras smoke on `LEN-GB000000003` posted full harvest `E000014`, completion `E000015`, completion reversal `E000016`, and harvest reversal `E000017`, restoring `20 EA`, `40 KG`, active status, zero costs, and QA output stock to zero. The production review found Portuguese G5.2 lifecycle copy leakage; copy-only commit `bc22eb3facd166dbcd59fb7d5bedb21bb51d20b9` passed Validation run `29051595028` and deployed as `dpl_BRA6QUesB64T8LwF3rUAF7dYFKfv`. | Treat G5.2 completion preview, lifecycle closeout, completion history, and event-specific completion reversal as live. Completion/reversal remain lifecycle-only: no stock movement or stock-level mutation, no quantity/weight/cost/price change, and no sale, invoice, COGS, FIFO, fair value, finance, profitability, split/child-batch, or whole-batch-reversal behavior. Production replay, mismatch, authority-negative, insufficient-state, and concurrency testing remain guarded local regression coverage. |

## Implementation Notes

- When a phase item is completed, add a new dated row instead of rewriting older decisions.
- If a prior decision is superseded, add a new row that explicitly replaces it and reference the older decision in the description.
- Keep this log focused on architecture and behavior decisions, not generic activity summaries.

| 2026-07-11 | Payment proof upload is evidence intake, not payment verification or entitlement authority. | Screenshots and PDFs cannot establish settlement truth without provider verification. The first release therefore uses private proof, immutable request history, platform-admin review, and atomic reuse of the existing access control plane. | Keep the package local-only until authorised rollout. Do not activate on upload, auto-send confirmation email, create finance ledger rows, or store provider secrets. Actual provider webhooks and automatic reconciliation remain future scope. |

